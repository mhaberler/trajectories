# Install: sudo cp deploy/mosquitto-websockets.conf.example /etc/mosquitto/conf.d/websockets.conf
#          sudo systemctl restart mosquitto
# Caddy terminates TLS and proxies /ws and /mqtt to this listener.

listener 1884 127.0.0.1
protocol websockets
